name: Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate static preview source
        run: |
          test -f index.html
          test -f app.js
          test -f styles.css

      - name: Print release download page
        run: |
          echo "Downloads: https://github.com/${{ github.repository }}/releases/latest"
